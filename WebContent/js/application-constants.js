const applicationContext = '/e-MitraSmartAgent';
const Globals = {
		MAX_UPLOAD_SIZE : 2 // In mega-bytes
}
const Messages = {
		EXCEEDS_MAX_UPLOAD_SIZE : 'The provided file exceeds max limit : 2 Megabytes'
}
var currentStep = 1;